1. 自我介绍
2. 你认为这个 nextjs 它解决的是这个纯 react 开发框架哪些核心的这个难点？
3. 这个 next js 13 版本之后的这个路由架构你了解吗？
4. App Router 当中这个动态路由怎么实现啊？
5. 这个 layout js 跟 template js 中间区别能讲一下吗？
6. Nestjs 实现路由跳转方式有哪些，能列举一下吗？
7. 数据获取方面，get server side props 这个适用场景是什么？
8. App router 当中有哪些获取数据的方式？
9. 静态再生成这个能讲一下吗？
10. 其他三种渲染方式可以再讲一下吗？还有它们的这个适用场景
11. 讲一下那个 nextjs 内置的图片优化能力
12. 讲一下这个动态导入吧，Dynamic import
13. 讲一下这个 middleware 中间件吧
14. Next.js 支持哪些部署方式啊？
15. 如何用这个 app router 来自定义 404 错误界面？
16. 找错误

``` JavaScript
// app/counter/page.js
import { useState } from 'react';
export default function CounterPage() {
    const [count, setCount] = useState(0);
    return (
        计数：{count}
        <button onClick={() => setCount(count + 1)}>增加
        
    );
}
```

18. 找错误
``` JavaScript
// app/profile/page.js
'use client'
import { useRouter } from 'next/navigation';
export default function ProfilePage() {
    const router = useRouter();
    return <button onClick={() => router.push('/home')}>返回首页</button>;
}

```

19. 找错误
``` JavaScript
// app/blog/layout.js
'use client'
import { useSearchParams } from 'next/navigation'

export default function BlogLayout({ children }) {
    const searchParams = useSearchParams()
    const keyword = searchParams.get('keyword') || '';
    return (
        <div>
            <header>当前搜索：{keyword}</header>
            {children}
        </div>
    );
}

```

20. 找错误
``` JavaScript
// app/product/[id]/page.js
export default async function ProductPage({ params }) {
    const res = await fetch(`https://api.example.com/product/${params.id}`, {
        cache: 'no‑store'
    });
    const product = await res.json();
    return <div>商品价格：{product.price}</div>;
}

```

21. 找错误
``` JavaScript
import { generateMetadata } from 'next/navigation';

export async function generateMetadata({ params }) {
    return { title: `文章 ${params.id}` };
}

export default function ArticlePage() {
    return <div>文章内容</div>;
}

```

22. 找错误
``` JavaScript
// app/comment/page.js（服务端组件）
async function handleSubmit(formData) {
    'use server'
    const content = formData.get('content');
    await db.comment.create({ data: { content } });
}

export default function CommentPage() {
    return (
        <form action={handleSubmit}>
            <textarea name="content" />
            <button type="submit">发表评论</button>
        </form>
    );
}
```

23. 找错误
``` JavaScript
// app/admin/page.js
import { redirect } from 'next/navigation';
export default async function AdminPage() {
    let user;
    try {
        user = await getCurrentUser();
    } catch (err) {
        return <div>加载失败：{err.message}</div>;
    }

    if (!user.isAdmin) {
        redirect('/login');
    }
    return <div>管理员后台</div>;
}

```

24. 找错误
``` JavaScript
'use client';
export default function UserInfo() {
    const apiKey = process.env.NEXT_PUBLIC_DB_API_KEY;
    return <div>接口密钥：{apiKey}</div>;
}

```

25. 找错误
``` JavaScript
// app/api/user/route.js
import { NextResponse } from 'next/server'

export async function GET(req) {
    const user = { name: '张三', age: 25 };
    return NextResponse.json(user, { status: 200 })
}

```

26. 其他方面还有没有我们没有 cover 到的这个技术站，然后你比较熟练的？
27. LangChain 你了解过吗？
28. Spring AI 有了解过吗？
29. 我看你还用过这个扣子平台啊，你觉得扣子平台问题在哪里？
30. 你有没有这个操作服务器的经验？
31. 有没有用过这个 docker 做过多服务器的集群？
32. 反问