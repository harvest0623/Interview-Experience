1. Next 你了解多少？我们公司主要用 Next，你有用这个写过项目吗？
2. 这个里面的 AI 对话，它里面提到说支持一句话需求解析为结构化形成，这个前端是怎么把用户输入的一句话传递给后端的呢？
3. 前端怎么把这个数据结构渲染成可视化的行程卡片呢？
4. 一个版本对比的功能，就是那你具体描述一下吧，如果用户修改了行程的话，前端怎么存储这个旧版本和新版本来支持这个对比的功能？
5. 这个项目用的是 Vue 是吧？如果是要用 Nextjs 来做的话，这个流式输出要怎么实现？
6. 这个 Next 里面，它的这个 server actions 能替代传统的 SSE 接口吗？
7. 这里面还涉及到高德地图啊，这个高德地图它的 API 是异步加载的，这里你是怎么处理呢？
8. 就 next 里面的这种服务端组件跟 vue、react 里面写的这些组件有什么不同？
9. 像这种 use client 这种需要交互的组件，跟其他有什么不同？
10. 提到这个路由，我再问一下 Next 里面的 middleware 是什么呢？
11. 你能再解释一下那几种渲染模式吗？SSR、CSR、ISR 等等这些。
12. 然后不用重新构建整个应用的情况下的话 ISR 是可以做到的吗？
13. 那如果我们要优化一个 Next 应用的首屏加载的性能，我有哪些优化方法呢？
14. 然后就是图片也可以优化，Next 是用什么组件来优化图片？
15. 使用的时候需要注意一些什么点呢？
16. 找出问题，说一下怎么修改怎么解决？
``` TypeScript
import Image from 'next/image'
export default function Banner() {
    return (
        <Image 
            src="https://cdn.example.com/banner.jpg"
            width={1920}
            height={1080}
            alt="网站横幅"
        />
    )
}
```
17. 找出问题并解决
``` TypeScript
// app/profile/page.jsx
import { useState } from 'react'
export default function ProfilePage() {
    const [isLiked, setIsLiked] = useState(false)  // ❌RSC不能用useState
    return (
        <div>
            <h1>用户资料</h1>
            <button onClick={() => setIsLiked(!isLiked)}> {/* ❌RSC不支持onClick事件 */}
                {isLiked ? '取消点赞' : '点赞'}
            </button>
        </div>
    )
}
```

18. 找出问题并解决
``` TypeScript
// app/actions.js
'use server'
import { revalidatePath } from 'next/cache'
export async function updateProduct(formData) {
    await saveProduct(formData)  // 假设这是保存数据库的函数
    
    // 试图刷新产品列表页
    revalidatePath('/products')
}
```

19. 找出问题并解决
``` TypeScript
// app/dashboard/page.jsx
import { Suspense } from 'react'
import Analytics from './Analytics'  // 耗时较长的组件
import Stats from './Stats'          // 耗时较长的组件
export default function Dashboard() {
    return (
        <Suspense fallback={<div>加载中...</div>}>
            <Analytics />
            <Stats />
        </Suspense>
    )
}

```

20. 第一个这个自己的项目里面用的是 Redis 来缓存来提升的性能是吧？
21. 项目里面提到多会话记忆管理啊，你是怎么在这个 langgraph 里面做的这个管理，怎么管理这些不同会话的上下文的呢？
22. 你第一段实习提到 SSE，这种关于 SSE 连接意外断开这种情况，那前端是怎么检测到它断开了呢？
23. 做过部署吗？工程化的这种部署。